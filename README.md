Before testing this, be sure to fix the module as per PR [#661](https://github.com/nuxt/image/pull/661)

Just run `yarn generate` and see the huge loop it creates in the output. This seems to be related to the `format="webp"` option
